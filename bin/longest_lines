#!/usr/bin/env ruby

table = Hash.new([])
ARGV.each do |filename|
  IO.readlines(filename).each_with_index do |line, index|
    table[line.length] += ["#{filename}:#{index+1}: #{line}"]
  end
end

table.sort.reverse.first(10).each do |index, text|
  puts text
end
